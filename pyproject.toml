[project]
name = "nrevah-pyvalkey"
version = "0.0.5"
description = "pyvalkey: Python implemented Valkey server"
readme = "README.md"
authors = [
    { name = "Netanel Revah" },
]
requires-python = ">=3.11"
dependencies = [
    "psutil>=7.1.3",
    "sortedcontainers>=2.4.0",
    "typer>=0.15.1",
]

[project.optional-dependencies]
lua = [
    "nrevah-pyvalkey-lua-runtime",
]
valkey-tests = [
    "nrevah-valkey-tests",
]

[tool.uv.build-backend]
module-name = "pyvalkey"
module-root = ""

[project.scripts]
valkey-server = "pyvalkey:main"

[build-system]
requires = ["uv_build>=0.8.5,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "docker>=7.1.0",
    "mypy>=1.15.0",
    "pytest>=8.3.4",
    "pytest-parametrization>=2022.2.1",
    "ruff>=0.9.6",
    "sortedcontainers-stubs>=2.4.2",
    "valkey>=6.1.0",
    "nrevah-pyvalkey-lua-runtime",
    "pytest-asyncio>=1.2.0",
    "ty>=0.0.1a23",
    "types-docker>=7.1.0.20251129",
    "types-psutil>=7.1.3.20251211",
    "cython>=3.2.4",
    "pre-commit>=4.0.0",
]



[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]

testpaths = "tests"

markers = [
    "slow",
    "string",
    "scripting",
    "acl",
    "protocol",
]

[tool.ruff]
target-version = "py311"
line-length = 120

lint.select = [
    "ANN",
    "E",
    "F",
    "FA",
    # "FBT",
    "FURB",
    "I",
    "N",
    "PGH",
    "PERF",
    "PL",
    "RUF",
    # "TRY",
    "UP",
    "YTT",
    "TC",
]
lint.ignore = [
    "E731",
    "RUF009",
    "PLR0911",
    "PLR0912",
    "PLR0913",
    "PLR0915",
    "PLW2901",
]

[tool.ruff.lint.per-file-ignores]
"**/{tests}/*" = ["ANN", "PLR2004", "PLR0913", "E501"]
"lua_runtime/**/*.pyi" = ["ANN401"]


[[tool.mypy.overrides]]
module = [
    "sortedcontainers",
    "typer",
]
ignore_missing_imports = true


